# Python package dependencies are provided by the upstream unitree_webrtc_connect install.
#
# The web video preview uses unitree_webrtc_connect's WebRTC video track and
# OpenCV only for JPEG encoding. The upstream project already depends on
# opencv-python. If cv2 is missing, install one of:
#
#   python -m pip install opencv-python
#   sudo apt install -y python3-opencv
